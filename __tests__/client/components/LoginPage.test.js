// import LoginPage from '../../../client/components/LoginPage/index';

describe('<LoginPage />', () => {
  it('should have a decent test', () => {
    expect('a string').toEqual('a string');
  });
});
